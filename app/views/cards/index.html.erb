<p style="color: green"><%= notice %></p>

<h1>Cards</h1>

<div id="cards">
  <% @cards.each do |card| %>
    <%= render card %>
    <p>
      <%= link_to "Back to user decks", user_decks_url %>
    </p>
  <% end %>
</div>

<%= link_to "New card", new_user_deck_card_path %>
